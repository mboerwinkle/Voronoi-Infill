fix dat thing where the display doesnt display every line
make not every layer be generated
make ORBitarray test suite
make genBitarray/setBit/getBit macros?
make binary search layer gen
add in physics movement
make calcCenter directly create nodes
make sure functions are passed by reference
make sure functions decrement to 0, not increment to a value(to check if neccessary)
make nodes reference each other by index, not pointer (problems when reallocing)
